import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Avaliation extends Component {
  const
  render() {
    return(
      <form></form>
    );
  }
}

Avaliation.propTypes = {
  xxxxx: PropTypes.string.isRequired,
};

export default Avaliation